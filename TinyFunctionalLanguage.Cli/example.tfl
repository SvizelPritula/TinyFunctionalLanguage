func main(): Point {
    let point = choose(false, Point(10, 20), Point(20, 10));
    Point(-point.x, -point.x)
}

func choose(cond: bool, a: Point, b: Point): Point {
    if cond { a } else { b }
}

struct Point {
    x: int;
    y: int;
}
