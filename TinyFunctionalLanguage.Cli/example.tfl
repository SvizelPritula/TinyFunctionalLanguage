func main(n: int): int {
    fib(0, 1, n)
}

func fib(a: int, b: int, n: int): int {
    let x = if n <= 0 {
        a
    } else {
        fib(b, a + b, n - 1)
    };

    if x > 1000000 {
        x = 1000000;
    };

    x
}
